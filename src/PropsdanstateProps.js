import React from 'react'

export const PropsdanstateProps = (props) => {
    return (
        <div>
            <h2>Propsdanstate | props</h2>
            <h1>{props.judul}</h1>
        </div>
    )
}
